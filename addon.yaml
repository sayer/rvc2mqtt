name: "RVC to MQTT Bridge"
version: "0.1.1"
slug: "rvc2mqtt"
description: "Bridging RVC CAN bus data to MQTT for Home Assistant."
arch:
  - aarch64
  - amd64
  - armv7
  - i386
hassio_api: true
url: "https://github.com/stephenayers/rvc2mqtt" # Replace with your add-on repository
startup: application # use this if the addon does not require system startup services.
image: "stephenayers/rvc-mqtt:latest"
icon: "mdi:car-connected" # Or use your desired icon
